# Reference outputs

This directory contains reports generated while developing the formalism note.  They are included for comparison and auditability.  To regenerate current outputs, run

```bash
python run_all_checks.py --clean
```

and inspect `outputs/latest/`.
